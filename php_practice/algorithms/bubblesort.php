<?php
 
function bubbleSort (array $arr):array{
 $swapped;
 $n = count($arr);
//  echo "the length of the $arr is $n" ;
 for ($i = 0; $i >= $n -1; $i++){
    $swapped = false;
    for (let )
    echo "$arr[$i], "; 
 }
  return $arr;
};
$arr = [1, 2, 3, 4];
bubbleSort($arr);
